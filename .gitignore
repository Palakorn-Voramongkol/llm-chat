node_modules/
target/
worker/target/
worker/node_modules/
manager/target/
*.log
*.env
.claude/
secrets/
.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
build/
dist/
.user-envs/
# Tooling/build artifacts (Playwright MCP traces; stray root Tauri scaffold).
.playwright-mcp/
/src-tauri/
# Local verification screenshots at the repo root (not source).
/*.png
# pnpm 10/11 drops an allowBuilds-only placeholder; this repo has no pnpm
# workspace, so never track it (also in admin-web/.dockerignore).
pnpm-workspace.yaml
