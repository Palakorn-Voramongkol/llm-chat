node_modules/
target/
worker/target/
worker/node_modules/
manager/target/
*.log
*.env
# Connection config — generated per-environment from .env.local.example + secrets/
# by the reset scripts. Local only; the committed template is .env.local.example.
.env.local
.claude/
secrets/
.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
build/
dist/
.user-envs/
# Tooling/build artifacts (Playwright MCP traces; stray root Tauri scaffold).
.playwright-mcp/
# Visual brainstorming companion (mockups + session state) — local only.
.superpowers/
/src-tauri/
# Local verification screenshots at the repo root (not source).
/*.png
/*.jpeg
/*.jpg
# pnpm 10/11 drops an allowBuilds-only placeholder; this repo has no pnpm
# workspace, so never track it (also in admin-web/.dockerignore).
pnpm-workspace.yaml
